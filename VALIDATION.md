# 验证记录

## 2026-09-09：Issue #2 同步覆盖（0.1.24 / build 25）

- 使用真实 `DesktopTaskCatalog` 和 `IslandModel` 复现：缓存存在 active 线索、IPC 已连接但无快照时，原逻辑返回“就绪”，回归断言失败。修复后显示“待同步”，确认数量仍为零；旧时间戳与近期时间戳均不被当成实时证据。
- `swift test -Xswiftc -warnings-as-errors`：47 项测试通过，无编译警告。覆盖有界重订阅、两个主机独立恢复、迟到快照、仅返回 notLoaded 时继续保持不确定性、完成待查看与已读计数、离线主机和账户切换；保留 #1 的合法增量活性与无响应过期回归。
- `swift build -c release --arch arm64 --arch x86_64 -Xswiftc -warnings-as-errors`：通过，`lipo` 确认双架构。
- 原生 SwiftUI 测试渲染验证“待同步”顶部提示、独立待同步区域、Windows 来源和不计入重点数量的说明。图片位于本地 `dist/validation/issue-2/pending-state.png`，使用测试数据。
- 真实只读诊断采样取得本机 24 条、远端 16 条目录记录，1 条当前 IPC 快照，待同步线索为 0；Codex Cloud 查询可用。分主机统计包含目录、有效快照、待同步、重试、过期原因与首次快照延迟。该次采样并未重现历史漏计现场，不能将其作为远端恢复的端到端证明。
- 独立 App Server proxy 初始化未连接成功，未验证到可替代 IPC 的独立远端元数据查询通路。本版没有利用宿主工具结果伪造实时状态，也未扩大认证、任务执行或所有权权限。
- 当前仍无有效 Apple 签名身份；未生成签名安装包、替换正在运行的应用或发布。真实网络中每个任务的快照可用性、睡眠恢复及实际远端状态转换未做全面验收。

## 2026-09-09：多来源任务（0.1.23）

- `swift test`：43 项 XCTest 全部通过。新增覆盖远程缓存与 Windows 路径、机器名称解析、ChatGPT 账户隔离、云端未读证据、状态过期与账户切换，以及真实 Unix socket 测试服务下的跨主机订阅、增量重订阅、归档恢复和版本拒绝。
- `swift build -c release --arch arm64 --arch x86_64`：通过；`lipo -archs` 确认同时包含 x86_64 和 arm64。保留了原有面板动画代码的 Swift 并发警告，本次没有新增警告。
- 原生 SwiftUI 渲染截图覆盖本机、Windows、SSH 长机器名、Codex Cloud、ChatGPT Work 缓存与离线机器。截图来自明确的测试数据，位于本地 `dist/validation/task-sources.png`；不作为远程实时状态的证明。
- 原生测试进程实际运行成功，窗口可见，合并目录为 60 条记录；测试进程已结束。
- Release 可执行程序 `--diagnose-sources` 实测：本机与远程目录共 36 条，其中远程 12 条；Codex Cloud 查询返回 20 条，当前账户 ChatGPT 缓存 4 条；成功读取 Windows、MacBook、Mac-mini 三个显示名。
- 实测取得 1 条 IPC 任务状态，远程实时状态为 0 条。远程条目因此显示“状态未同步”；跨主机协议处理经过测试服务验证，但尚未获得真实远程运行、等待确认及完成状态的端到端证据。
- ChatGPT 对话目录 HTTP 查询返回 403，未绕过权限；实现仅读取当前账户的客户端缓存，并明确标注没有实时状态。缓存之外的 ChatGPT / Work 对话没有覆盖。
- 当前签名检查为 **0 个有效身份**。本轮没有生成签名 `.app`、安装或发布，也没有用临时签名替代项目要求的 Apple 身份。此前版本的签名验证不能视为本轮已完成打包。
- 没有实际操作远程任务、修改远程文件、切换登录账户、配对设备或启动新任务。真实远程跳转、SSH / WSL 主机运行和 Intel 硬件运行仍未验证。

本轮日志与来源统计保存在本地 `dist/validation/`，不进入 Git；统计不包含任务标题、消息或凭据。

## 2026-09-08：历史源码包验证

版本：0.1.22。日期：2026-09-08。

### 干净源码验证

将源码复制到独立目录，排除 `.build/`、`dist/` 和 Git 元数据；从该干净副本执行：

```sh
swift test
bash scripts/build-app.sh
codesign --verify --deep --strict --verbose=2 "dist/Codex Island.app"
```

- 34 项 XCTest 全部通过，包含核心协议、任务状态、额度解析、面板布局与刷新反馈回归。
- 应用构建完成，包含 arm64 与 x86_64 两种架构。
- 默认产物位置为当前仓库的 `dist/Codex Island.app`，严格签名校验通过。
- 验证环境：Apple Silicon、macOS 26.6.2、Apple Swift 6.3.3；打包使用验证机器已有的 Apple Development 身份。
- 构建脚本及可选插件脚本通过 Bash 语法检查。
- 源码归档只包含维护文件，无构建缓存、预编译应用、日志、账户配置、证书或私钥。
- 检查了已知个人用户名、账户邮箱、个人签名标识及私钥标记，源码中没有匹配；此检查不等于完整安全审计。

### 当时已有的运行验证

应用在当前 MacBook 的内置刘海屏上使用 Codex Desktop 26.901.51231 运行。收起与展开、重点任务数量、黄色提示点、额度读数与刷新、显示额度开关及重启保存已有本机验证。加载图标可见性通过黑色面板栅格渲染回归；没有逐帧屏幕录像测量。

### 当时未验证的范围

- 没有在另一台全新 Mac、Intel Mac 或最低支持版本 macOS 14 上运行测试。含 Intel 架构不代表已经在 Intel 硬件验证。
- Apple Development 签名不等于 Developer ID 分发签名和公证。构建脚本没有执行公证；此前二进制的 Gatekeeper 分发评估未通过。
- 未对实际断网、退出登录和额度重置进行破坏性验证，相关分支通过模拟响应及有效期回归覆盖。
- 任务状态使用内部 IPC v11，Codex 客户端升级后可能需要适配；没有覆盖所有客户端版本、远程主机或云端任务。

只重新执行与本次干净源码构建相关的验证，未启动第二个常驻应用实例或改变正在运行应用的设置。
