# 源码包验证记录

版本：0.1.22。日期：2026-09-08。

## 本次验证

将源码复制到独立目录，排除 `.build/`、`dist/` 和 Git 元数据；从该干净副本执行：

```sh
swift test
bash scripts/build-app.sh
codesign --verify --deep --strict --verbose=2 "dist/Codex Island.app"
```

- 34 项 XCTest 全部通过，包含核心协议、任务状态、额度解析、面板布局与刷新反馈回归。
- 应用构建完成，包含 arm64 与 x86_64 两种架构。
- 默认产物位置为当前仓库的 `dist/Codex Island.app`，严格签名校验通过。
- 验证环境：Apple Silicon、macOS 26.6.2、Apple Swift 6.3.3；打包使用验证机器已有的 Apple Development 身份。
- 构建脚本及可选插件脚本通过 Bash 语法检查。
- 源码归档只包含维护文件，无构建缓存、预编译应用、日志、账户配置、证书或私钥。
- 检查了已知个人用户名、账户邮箱、个人签名标识及私钥标记，源码中没有匹配；此检查不等于完整安全审计。

## 已有运行验证

应用在当前 MacBook 的内置刘海屏上使用 Codex Desktop 26.901.51231 运行。收起与展开、重点任务数量、黄色提示点、额度读数与刷新、显示额度开关及重启保存已有本机验证。加载图标可见性通过黑色面板栅格渲染回归；没有逐帧屏幕录像测量。

## 未验证范围

- 没有在另一台全新 Mac、Intel Mac 或最低支持版本 macOS 14 上运行测试。含 Intel 架构不代表已经在 Intel 硬件验证。
- Apple Development 签名不等于 Developer ID 分发签名和公证。构建脚本没有执行公证；此前二进制的 Gatekeeper 分发评估未通过。
- 未对实际断网、退出登录和额度重置进行破坏性验证，相关分支通过模拟响应及有效期回归覆盖。
- 任务状态使用内部 IPC v11，Codex 客户端升级后可能需要适配；没有覆盖所有客户端版本、远程主机或云端任务。

只重新执行与本次干净源码构建相关的验证，未启动第二个常驻应用实例或改变正在运行应用的设置。
